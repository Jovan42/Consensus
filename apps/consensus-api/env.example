# Database
# Use the same database for both local development and production
# Replace with your actual Render PostgreSQL connection string:
DATABASE_URL=postgresql://consensus_user:your_password@hostname:port/consensus_db

# Server
PORT=3001
NODE_ENV=development

# CORS
FRONTEND_URL=http://localhost:3000
